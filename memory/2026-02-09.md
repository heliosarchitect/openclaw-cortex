# 2026-02-09 — Daily Log

## Major Accomplishments
- **Security Assessment**: 13 findings (SEC-1 through SEC-13) documented in `reports/security/2026-02-09-vulnerability-assessment.md`
- **Vision Docs Restructured**: Monolithic HELIOS_VISION.md → 6 focused ITIL-aligned docs under `vision/`
- **Ansible Fleet Built**: inventory, group_vars, 6 task playbooks + bootstrap-sudo, site.yml master
- **Hardening Applied**: SSH, auditd, fail2ban, firewall on .104, .107, .143 (then SSH lockout, then fixed)
- **ITSM Service Monitoring**: Per-service status checks on /itsm page — 20 services across 5 servers
- **Wazuh→Discord Alerting**: 1,324 alerts processed, posted to #system-health
- **QA Sweep**: 15 tests, timestamps fixed to EST, collector memory check improved
- **Skylight CLI**: Built `skylight.py` — list/add/done/remove for all 3 lists
- **Jennifer Onboarded**: Signal + Skylight (Grocery + Farm Store lists)
- **Docker Cleanup**: Removed OpenProject + Dolibarr from .104 (7 dead containers)
- **Network Scan**: 30+ devices inventoried on 192.168.10.0/24

## SSH Lockout Incident
- Ansible changed SSH port to 2222 + enabled firewall blocking 22
- ICMP task failure aborted play before handlers fired → sshd never restarted
- .107 and .143 locked out, Matthew fixed via physical console
- **Lesson**: Never change SSH port + enable firewall without flush_handlers
- **Fix**: Reordered playbook — firewall opens new port BEFORE sshd restarts

## Evening Session (22:00–23:00)
- **Cortex Config Applied**: hotTierSize 100→500, activeSessionCapacity 50→200, maxContextTokens 1500→2000, truncateOldMemoriesTo 180→250. Key learning: config goes under `plugins.entries.cortex.config`, NOT directly under the entry.
- **Cortex Config Reference**: Full doc at `reference/cortex-config.md` — all settings, types, ranges, defaults, cost analysis
- **LCARS Cortex Panels**: Two new panels on `/itsm` — "Helios AI — Cortex Memory" (embeddings/STM/atoms/links) + "Cortex Configuration" (tokens/hot tier/session cache). Reads from SQLite directly.
- **Scoped Memory Architecture**: Added Phase 2.5 to `vision/helios-program.md` — 5 tasks for memory isolation (private/group/shared scoping). Default: private. Decision: vision only, not sprint.
- **Dashboard commit**: `6d2276c` — feat: Cortex memory stats panel on ITSM dashboard

## Blocked / Pending
- **giggletits /boot full**: 34MB free, dpkg broken (can't build initramfs). Need `sudo apt remove linux-image-6.14.0-33-generic`
- **NOPASSWD bootstrap**: Not run on remote servers. Blocks all Ansible without -K
- **Wazuh API password**: Still default wazuh-wui/wazuh-wui
- **giggletits SSH**: sshd not restarted to port 2222 yet
- **VS Code GPG key**: Stale, blocking apt on giggletits
- **Collector MemoryMax**: 2G too low, needs 8G
- **Pi-hole Web**: Port 80 blocked by ufw on .107

## Key Decisions
- Ansible over per-server scripts
- Run Ansible from giggletits
- NOPASSWD sudo for home lab
- Separate vision docs from security reports
- Skip iTop, build ITSM into LCARS dashboard
- Always post tasks to Skylight + Discord, not just chat

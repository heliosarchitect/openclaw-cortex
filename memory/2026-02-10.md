# 2026-02-10 — Daily Log

## Session Summary

### Infrastructure (00:00 - 01:00)
- Fixed AUGUR enhanced_db path (35K trades ran blind against wrong DB)
- Fixed .104 SSH (Docker DNAT intercepting port 2222 → changed Gitea SSH to 2223)
- Set up lbf.fleet.wood reverse proxy (nginx on .104 → Traefik → giggletits:8090)
- Fixed Pi-hole v6 DNS (fleet.wood records in /etc/hosts, toml broken by DHCP error)
- Hardened dashboard service (Restart=always, survived SIGHUP)
- Pushed all repos to Gitea (master→main branch mismatch was silent killer)
- Committed 61 files / 12,822 insertions to GitHub
- Task board: 24 → 9 open items

### LLM Fleet (01:00 - 02:00)
- Created all 7 Modelfiles on qwen2.5:32b: qa-sweep, log-analyzer, heartbeat-monitor, pattern-evaluator, ansible-writer, discord-classifier, email-triager
- All registered in Ollama and tested (qa-sweep returned valid JSON ✅)
- Added LLM Fleet panel to ITSM dashboard (/itsm) showing model status + VRAM
- Fixed auth proxy (nginx was stripping Authorization header)
- Created Modelfile TEMPLATE with every parameter documented
- Updated vision doc from "cost optimization" to "concurrent daemon architecture"
- Pushed llm-fleet repo to Gitea

### Task Board Progress (01:30 - 02:00)
- Built claude-usage-tracker.py (reads session JSONLs for cost data)
- Sub-agents delivered: expenditure tracker (Google Sheets), tax research report
- Wrote bottleneck-tracking vision doc (tool→infrastructure graduation)
- Spawned researcher for local coding model comparison
- 10 → 7 open tasks

### Cortex Maintenance (02:00)
- Fixed nightly maintenance: tz-aware datetime comparison + surrogate char encoding
- 2,093 total memories indexed after sync

## Key Insights from Matthew

### 1. Concurrent Architecture, Not Sequential
GPU runs multiple small models simultaneously. 32GB ÷ 2GB = 16 concurrent specialists.
Not "swap one model at a time" — multiple daemons always resident, watching their domains.
Security agent + QA agent + email triage + XTTS all loaded at once.

### 2. Token Offload Rate is the Metric
Not VRAM efficiency, not dollars per model. Every token running locally = money saved.
Maximize coverage (more task types handled locally), not per-model quality.

### 3. Dynamic VRAM — Don't Shoehorn
Ollama evicts idle models automatically. Full 32GB available to active model.
Small daemons stay resident; large models (qwen2.5:32b) swap in for complex tasks.
Don't think "what fits alongside X" — think "what's the fleet composition."

### 4. The Fleet Name Wasn't Accidental
fleet.wood → Fleetwood. Starfleet. A fleet of servers, models, agents.
Each specialist running their station on the bridge. The name was always the vision.

### 5. Sub-Agents on Local Models = 24/7 Free Productivity
"Imagine what you could do if you could run sub agents 24/7"
The API is the escalation path, not the default. Local = the operating system.

### Coding Fleet (02:30 - 03:30)
- Pulled qwen2.5-coder:7b (4.7GB, 84.1% HumanEval+) — recommended by research sub-agent
- Created 4 codex Modelfiles: codex-lint, codex-test, codex-review, codex-json
- All registered in Ollama, smoke tested (codex-lint caught hardcoded password + path traversal)
- Pushed llm-fleet to Gitea (was local-only)
- Closed task #25 (local coding model research) — board now at 5 open tasks
- Research report: 367 lines at analysis/local-coding-model-research.md

### Maintenance (02:30 - 03:45)
- Nightly backup to Google Drive (fixed --account flag in backup script)
- Overnight reflections written to memory/reflections.md
- Learned about Lobster workflow runtime (multi-step pipelines with approval gates)
- Read cron-vs-heartbeat doc — identified optimization: batch email/world checks into heartbeat

## Fleet Status (as of 03:30)
- **11 Ollama models**: 7 classification (qwen2.5:32b) + 4 coding (qwen2.5-coder:7b)
- **5 open tasks**: #11 collector refactor, #15 temporal split, #16 pattern lifecycle, #17 Discord+OpenClaw, #20 bottleneck tracking
- **All services healthy**: paper-augur, enhanced-collector, augur-dashboard, openclaw-gateway, cortex-embeddings

### AUGUR Bug Fixes (05:00 - 07:00)
Deep analysis (analysis/augur-deep-analysis.md) identified 5 critical bugs. All 4 code bugs fixed in paper_augur.py, live trader built:

**Bug 1: Cross-product pattern matching** — `if product not in name` was substring match (ETH-USD matched CBETH-USD patterns). Fixed: exact match on `conditions.get('product')` with fallback to `name.endswith(product)`.

**Bug 2: Missing indicators** — 50% of loaded patterns (182/363) referenced `imbalance_ma`, `volume_proxy`, `price_ret_60` which `get_orderbook_state()` didn't compute. Fixed: added all three calculations from the 60-snapshot window.

**Bug 3: Exit strategy mismatch** — All patterns used fixed 300s max hold regardless of backtested lookahead. Fixed: `max_hold` now reads from `conditions.get('lookahead', 300)` so 30s patterns exit at 30s, 600s patterns hold 600s.

**Bug 4: Compound pattern handling** — Compound patterns (multiple conditions) were silently skipped. Fixed: full compound pattern evaluation with operator support (`>`, `<`, `>=`, `<=`) and liquidity filter.

**Bug 5: Enhanced DB wrong path** — Fixed ~00:23 (earlier in session). `ENHANCED_DB` pointed to non-existent `Chad_Volume_tracker/` path. Changed to `augur-collector/enhanced_data.db`.

**Live trader built:** `live_augur.py` (12KB) — conservative mode:
- Uses Coinbase Advanced Trade API via CHADSQUARED key (`~/Projects/augur-collector/.env`)
- Safety limits: $5/trade, 1 trade/hour, $50/day max exposure
- Min 65% WR + 100 occurrences to qualify
- Kill switch: `touch /tmp/augur-live-stop`
- Signal bridge: reads `live_signal.json` emitted by paper_augur.py
- NOT running live yet — built and ready for when paper results validate

**Current state:**
- 27,919 total patterns in patterns.db
- 738 pass WR≥60% + occurrences≥100 filter
- 363 after deduplication (best percentile per indicator×direction×product)
- Paper trader restarted with all fixes, DB should be reset for clean validation
- `_emit_live_signal()` added to paper_augur.py — writes JSON for live trader

### Sub-Agent Results (07:15)
**augur-code-review** — paper_augur.py fixes all correct ✅. live_augur.py has 3 crash-level bugs:
1. Wrong CoinbaseClient constructor (expects CoinbaseAuth, not raw strings)
2. No aiohttp session init (all API calls would fail)
3. Signal bridge not wired (main loop never reads live_signal.json)
Also found subtle compound pattern bug: unrecognized operators silently pass (not a live issue today).

**augur-docs-sync** — Updated 4 files: daily log, MEMORY.md, AUGUR README (full rewrite v0.3.0), deep analysis (added resolution section).

### Tasks Posted (07:00)
- Skylight: Twilio auto-recharge, AUGUR live limits review, BLISS Pi check
- Discord #general: Same 4 tasks (Twilio, AUGUR limits, BLISS Pi, live trading approval)

### BLISS Pi Onboarding (07:30 - 08:25)
- Created `bonsaihorn` user with NOPASSWD sudo, deployed SSH key
- Fixed hostname (`127.0.1.1 bliss`, `hostnamectl set-hostname bliss`)
- Ansible playbook `site.yml --limit bliss`: SSH port 2222 ✅, UFW ✅, auditd ✅
- fail2ban installed + enabled ✅
- Wazuh agent 4.8.0 installed, enrolled as Agent #005 — Active ✅
- Added wazuh-agent task to Ansible playbook (import_tasks, version-matched to manager)
- Fleet status: 5/5 servers with Wazuh agents active

### AUGUR Post-Fix Results (08:25)
- 89 trades in ~1.5hrs post-fix: **20.2% WR, -8.9% PnL** across 60 products
- WORSE than pre-fix 43.4% WR — correct product matching exposed weak patterns
- Patterns are scattered (1-2 trades per product), no concentration
- **Conclusion: pattern discovery itself is flawed, finding noise not signal**

### Live Trader Status
- Running but only caught 1 signal (LTC-USD, rejected: 54.3% WR < 65% threshold)
- 3 crash bugs fixed earlier (CoinbaseAuth, aiohttp, signal bridge)
- NOT recommended for live trading at current WR

### AUGUR V2 & Backtesting (08:00 - 09:50)
- Discovered Coinbase VIP 2 fee tier: 0.10% taker, 0.05% maker ($1.47M volume)
- Train/test validated NKN-USD stacked signals: Stack≥3 60s 56.6%WR +0.10% net ✅
- Built V2 real-time scanner (sub-agent), then refactored to use collector DB (no WebSocket)
- V2 scanner running dry-run: 229 products, polling trade_flow every 5s
- **V2 signal quality issue**: 998 signals in 24 min (~42/min) — way too many, fr=99.0 (zero sell volume bars) not real signal
- Monday backtest (9AM-6PM ET): aggregate -324.92% but top products profitable (NKN +10.6%, ZKP +10.2%, BNKR +8.4%)
- 300s hold best, 15-30s universally negative — tension with Matthew's "even 10 min too long" preference
- Big caps (BTC/ETH/SOL/XRP) consistently worst — mid-caps where alpha lives
- BLISS Pi fully enterprise-managed (Ansible, Wazuh agent #005, SSH port 2222)
- Matthew corrected: data includes Monday (Feb 9), not just weekend. It's Tuesday Feb 10.

## Pending
- [ ] **Fix V2 scanner signal quality** — add minimum volume filter, higher flow ratio thresholds, or fr=99 filtering
- [ ] Wire Modelfiles into actual agent workflows (LLM-10)
- [ ] Build daemon architecture (always-on small model agents)
- [ ] 5 remaining engineering tasks (#11, #15, #16, #17, #20)
- [ ] Build pattern funnel (P1 from deep analysis)
- [ ] Add time-of-day gating (H20 was catastrophic)
- [ ] Brief Matthew on Twilio $20 auto-recharge + his cancellation email
- [ ] **Rethink pattern discovery** — need V2 approach with stacked signals across all products

## Handoff Notes (pre-compaction ~07:30)
- Paper trader running clean with all 4 fixes since ~06:50. Need 1-2h of data to see if WR improves from 34.5% baseline.
- Live trading NOT safe to start — live_augur.py needs 3 critical fixes first.
- Matthew asleep. Tasks waiting for him on Skylight + Discord.
- Twilio situation needs attention when he wakes.
- Working memory pins: sub-agent announce policy (always NO_REPLY), H0 status (H0-4/5/6 remain).
- Overnight: M5.0 Jamaica, M5.1 Kamchatka (both under 6.0 threshold). No email action items.
- Discord bot token: raw token used for posting (discord-post.sh lacks arbitrary channel support).

## 11:15 — Signal Discovery Complete
- Sub-agent exhaustive search completed (aborted at timeout but got results written)
- 612 validated signals across 11 products (train/test split, 0.20% RT fees)
- Best: NKN-USD zscore_60 below p10 → LONG 300s, 59%WR, +1.4% net per trade
- Zero signals work on big caps (BTC/ETH/SOL/XRP)
- All profitable signals require 300s hold — nothing at shorter timeframes
- Vision doc disconnect identified: was hardcoding 4 indicators instead of running discovery engine
- Matthew frustrated — wants FIXES not analysis. Launched exhaustive search in response.

## 11:41 — Short-Hold Deep Dive
- Matthew: "If nothing is profitable at short holds you aren't finding a solution properly"
- Ran exhaustive short-hold analysis: 115,200+ combinations, 20 products, 15/30/60s holds
- **Physics of short holds**: At 15s, 57% of NKN-USD price moves are INSIDE the 0.20% fee band. Even perfect foresight yields only 21% WR.
- At 60s: 33% inside band. Features discriminate direction (book_imb: 38% vs 21% up-rate between quintiles) but not enough to profit after fees.
- Sub-agent found 83 signals at relaxed criteria, but **NKN-USD spread is 1.08%** — 5x the exchange fee. Mid-price backtests are fantasy.
- **Book imbalance IS genuinely predictive** — monotonic, consistent. Best use: entry timing for 300s holds, not standalone short-hold signals.
- Tested: volume surge + flow, momentum + book, spread compression + volume, mean reversion, volatility conditioning, Monday-only, maker fees
- Result: Zero validated signals survive at taker fees on any product at any short hold
- Paths forward: lower-fee venue, 300s holds on mid-caps, or book_imb as entry timing enhancer
- Reports: `analysis/augur-short-hold-discovery.md`

## V2 Scanner Updates (11:00-12:00)
- Added trend filter: only fire signals on products with positive 60-bar price trend
- 88 products trending UP blocked from spurious SHORT signals
- Found SKR-USD and ZKP-USD profitable with consecutive bar signals
- Scanner still needs volume floor fix for fr=99 noise

## 13:00-13:37 — AUGUR Strategic Pivot + Live Trading

### Matthew's Key Insight
- "You aren't finding trends! There are billions of combinations!"
- "That is the whole point! It isn't something we can see!"
- "AUGUR has all this data — MINE IT, find the trend!"
- Stopped hand-picking indicators → built combinatorial discovery engine

### Signal Miner V2 Built & Run
- 72 features computed per product (price returns, z-scores, volume surges, OB imbalance, EMA crossovers, flow ratios, consecutive direction, composites)
- 50 products scanned, singles + pairs tested with train/test validation
- **1,432 validated signals across 8 products in under 60 seconds**
- Key discovery: `mid_vwap_div` (VWAP vs OB mid price divergence) — most predictive feature, wasn't tested before
- Script: ~/Projects/augur-trading/signal_miner_v2.py

### Deep Mining Sub-Agent Results
- All 204 products: 1,455 signals on 15 products
- Multi-hold: 1800s produces 7,857 signals vs 2,045 at 60s
- MON-USD hidden gem at 1800s hold
- 13,064 triple combos validated
- Report: analysis/augur-deep-mining-results.md

### Live Trading V2 (killed)
- 4 trades: NKN +$0.13, GHST -$0.17, GHST -$0.32, NKN -$0.06 = -$0.42 total
- Orphaned positions cleaned up (NKN 466.8, GHST 23.31 sold)

### Live Trading V3 (RUNNING)
- PID 2569041, 78 products monitored for mid_vwap_div signals
- 4 positions entered at 13:34: ETH, SOL, XRP, ZEC
- Exits expected at ~13:39 (300s hold)
- $5/trade, 4/hr max, $50/day loss limit

### Context Reset Prep
- All state dumped to Cortex (5 memories, importance 2.5-3.0)
- Daily log updated
- V3 trader running autonomously

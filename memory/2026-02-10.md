# 2026-02-10 — Daily Log

## Session Summary

### Infrastructure (00:00 - 01:00)
- Fixed AUGUR enhanced_db path (35K trades ran blind against wrong DB)
- Fixed .104 SSH (Docker DNAT intercepting port 2222 → changed Gitea SSH to 2223)
- Set up lbf.fleet.wood reverse proxy (nginx on .104 → Traefik → giggletits:8090)
- Fixed Pi-hole v6 DNS (fleet.wood records in /etc/hosts, toml broken by DHCP error)
- Hardened dashboard service (Restart=always, survived SIGHUP)
- Pushed all repos to Gitea (master→main branch mismatch was silent killer)
- Committed 61 files / 12,822 insertions to GitHub
- Task board: 24 → 9 open items

### LLM Fleet (01:00 - 02:00)
- Created all 7 Modelfiles on qwen2.5:32b: qa-sweep, log-analyzer, heartbeat-monitor, pattern-evaluator, ansible-writer, discord-classifier, email-triager
- All registered in Ollama and tested (qa-sweep returned valid JSON ✅)
- Added LLM Fleet panel to ITSM dashboard (/itsm) showing model status + VRAM
- Fixed auth proxy (nginx was stripping Authorization header)
- Created Modelfile TEMPLATE with every parameter documented
- Updated vision doc from "cost optimization" to "concurrent daemon architecture"
- Pushed llm-fleet repo to Gitea

### Task Board Progress (01:30 - 02:00)
- Built claude-usage-tracker.py (reads session JSONLs for cost data)
- Sub-agents delivered: expenditure tracker (Google Sheets), tax research report
- Wrote bottleneck-tracking vision doc (tool→infrastructure graduation)
- Spawned researcher for local coding model comparison
- 10 → 7 open tasks

### Cortex Maintenance (02:00)
- Fixed nightly maintenance: tz-aware datetime comparison + surrogate char encoding
- 2,093 total memories indexed after sync

## Key Insights from Matthew

### 1. Concurrent Architecture, Not Sequential
GPU runs multiple small models simultaneously. 32GB ÷ 2GB = 16 concurrent specialists.
Not "swap one model at a time" — multiple daemons always resident, watching their domains.
Security agent + QA agent + email triage + XTTS all loaded at once.

### 2. Token Offload Rate is the Metric
Not VRAM efficiency, not dollars per model. Every token running locally = money saved.
Maximize coverage (more task types handled locally), not per-model quality.

### 3. Dynamic VRAM — Don't Shoehorn
Ollama evicts idle models automatically. Full 32GB available to active model.
Small daemons stay resident; large models (qwen2.5:32b) swap in for complex tasks.
Don't think "what fits alongside X" — think "what's the fleet composition."

### 4. The Fleet Name Wasn't Accidental
fleet.wood → Fleetwood. Starfleet. A fleet of servers, models, agents.
Each specialist running their station on the bridge. The name was always the vision.

### 5. Sub-Agents on Local Models = 24/7 Free Productivity
"Imagine what you could do if you could run sub agents 24/7"
The API is the escalation path, not the default. Local = the operating system.

## Pending
- [ ] Researcher sub-agent: local coding model comparison (in progress)
- [ ] Wire Modelfiles into actual agent workflows
- [ ] Build daemon architecture (always-on small model agents)
- [ ] 7 remaining engineering tasks
- [ ] AUGUR P1 fixes from deep analysis (cross-product matching, missing indicators)
